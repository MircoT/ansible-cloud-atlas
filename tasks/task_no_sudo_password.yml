- name: Allow no password with sudo
  hosts: localhost
  tasks:
    - name: Get the username running the deploy
      become: false
      local_action: command whoami
      register: username

    - name: Create {username} sudoers rule
      become: true
      copy:
        dest: "/etc/sudoers.d/{username}"
        content: |
          {username}  ALL=(ALL) NOPASSWD:ALL